# 变更日志

TjuaeCore 的重要变更都会记录在本文件中。

## 0.3.1 - 2026-08-04

- 补齐 GitHub App 端到端发布契约：授权后校验个人账户安装、全部仓库范围以及
  Administration、Contents、Pull requests 三项写权限。
- 缺少安装时保留加密凭据并返回受控安装入口，支持安装后原地重新检查；发布前再次校验，
  避免在创建 fork 或 PR 时才暴露权限错误。
- 正式构建同时校验 GitHub App Client ID 与安装地址，避免生成无法完成真实发布的二进制文件。

## 0.3.0 - 2026-08-04

- 建立 Core 本地资产库与 Hub 远程资产库分离的双资源库模型。
- 完成 assistant、engineAdapter、skill、mcp 四类 Definition、typed Overlay、加密凭据、
  Tracking/Base、事务投影、运行绑定和 Trace。
- 资产安装、同步、发布和 GitHub Device Flow 全部通过 Core REST 实现，应用运行时不依赖
  `gh` 或本机 `git`。
- 删除第三方 CLI 托管、下载与安装能力；引擎适配器只检测并连接用户已安装的命令。
- 官方助手与技能迁移到 TjuaeHub；缺网时只回退到经过固定提交校验的离线种子。
- 临时会话目录使用逐目录单次信任注入，通知使用稳定错误码并由 UI 本地化。

## 0.2.0 - 2026-07-27

- 建立 TjuaeCore 产品身份与仓库契约。
- 将上游运行时名称、路径、协议、包名和发布产物替换为 Tjuae 对应名称。
- 移除上游数据库、配置、扩展和智能体标识的兼容回退。
- 移除推广助手、遥测与外部自动发版黑盒，建立身份、迁移、格式、静态检查、测试与安全审计门禁。
- 将 TjuaeCLI SDK 固定到可追溯的 `v0.3.0` 标签，不再依赖本地相邻目录。
